public enum SearchResultParkingRateCustomValue {
    /** 6 months Monday to Friday. */
    case sixMonthsMonFri
    /** Bank holiday rates. */
    case bankHoliday
    /** Daytime rates. */
    case daytime
    /** Early bird rates. */
    case earlyBird
    /** Evening rates. */
    case evening
    /** Flat rate pricing. */
    case flatRate
    /** Maximum rate. */
    case max
    /** Maximum rate applied only once. */
    case maxOnlyOnce
    /** Minimum rate. */
    case minimum
    /** Monthly rate. */
    case month
    /** Monthly rate Monday to Friday. */
    case monthMonFri
    /** Monthly reserved rate. */
    case monthReserved
    /** Monthly unreserved rate. */
    case monthUnreserved
    /** Overnight rate. */
    case overnight
    /** Quarterly rate Monday to Friday. */
    case quarterMonFri
    /** Rate until closing. */
    case untilClosing
    /** Weekend rate. */
    case weekend
    /** Yearly rate Monday to Friday. */
    case yearMonFri
}
